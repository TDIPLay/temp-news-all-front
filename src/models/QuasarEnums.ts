export enum InputType {
  text = 'text',
  password = 'password',
  textArea = 'textarea',
  email = 'email',
  search = 'search',
  tel = 'tel',
  file = 'file',
  number = 'number',
  url = 'url',
  time = 'time',
  date = 'date',
  // submit = 'Submit',
}

export enum MaskType {
  date = 'date',
  dateTime = 'datetime',
  time = 'time',
  fullTime = 'fulltime',
  phone = 'phone',
  card = 'card',
}

export enum DocType {
  img = '.png, .jpg, .jpeg, .gif',
  businessLicense = '.jpg, .png, .gif, .pdf',
  excel = '.xlsx',
  yaml = '.yaml',
}

export enum Size {
  xs = 'sx',
  sm = 'sm',
  md = 'md',
  lg = 'lg',
  xl = 'xl',
}
